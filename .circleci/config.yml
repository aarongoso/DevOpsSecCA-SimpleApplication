version: 2.1

jobs:
  build_and_test:
    docker:
      - image: cimg/node:20.17
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test

workflows:
  version: 2
  build_test_workflow:
    jobs:
      - build_and_test